# pybullet-rl-examples 项目分析报告

> **作者**：Sean Nishimiya ([22560063@zju.edu.cn](mailto:22560063@zju.edu.cn))
>
> **仓库**：[NishimiyaXSean/pybullet-rl-examples](https://github.com/NishimiyaXSean/pybullet-rl-examples)
>
> 本项目是一个基于物理仿真的**无人机1v1空战强化学习**研究平台。核心仿真环境基于 [gym-pybullet-drones](https://github.com/learnsyslab/gym-pybullet-drones)，作者在此基础上重点扩展了多智能体强化学习算法（MAPPO）和空战目标追踪逻辑。

---

## 目录

- [一、底层环境](#一底层环境)
- [二、文件架构](#二文件架构)
- [三、运用的工具和主要方法](#三运用的工具和主要方法)
  - [3.1 仿真与物理建模](#31-仿真与物理建模)
  - [3.2 强化学习方法](#32-强化学习方法)
  - [3.3 战术空战建模（BFM）](#33-战术空战建模bfm)
  - [3.4 奖励函数工程](#34-奖励函数工程)
  - [3.5 辅助工具](#35-辅助工具)
- [四、目前已做到的事项](#四目前已做到的事项)

---

## 一、底层环境

本项目采用分层架构设计，从底层物理引擎到上层强化学习框架形成了完整的技术栈。

| 层级 | 技术栈 |
|------|--------|
| **物理引擎** | **PyBullet 3.2.5+** —— 基于 Bullet Physics 的 Python 物理仿真引擎，负责飞行动力学、碰撞检测、3D 渲染 |
| **仿真环境接口** | **Gymnasium 1.2** —— OpenAI Gym 的继任者，提供标准化的 RL 环境 API（`reset` / `step` 等） |
| **强化学习框架（双轨）** | ① **Stable-Baselines3 2.0** —— 用于单智能体 PPO 基线训练（`examples/basic/`） ② **Ray RLlib** —— 用于多智能体 MAPPO 训练（支持分布式并行采样） |
| **深度学习后端** | **PyTorch**（通过 RLlib 的 `framework("torch")` 指定） |
| **数值计算** | **NumPy 2.2**, **SciPy 1.10**, **Transforms3D 0.4.1** |
| **可视化/日志** | **Matplotlib 3.7**, **TensorBoard 2.20**, **Tacview ACMI**（空战战术回放格式） |
| **控制理论库** | **Python Control 0.10.2** |
| **Python 版本** | **3.10**（通过 Conda 环境管理） |
| **包管理** | **Poetry**（`pyproject.toml`） |
| **基础代码来源** | 基于 [gym-pybullet-drones](https://github.com/learnsyslab/gym-pybullet-drones) 的仿真内核，在此基础上深度定制 |

---

## 二、文件架构

项目的文件组织遵循模块化设计原则：`utils/` 提供工具层，`control/` 封装飞控算法，`envs/` 定义 Gymnasium 环境，`examples/` 是核心研发区（也是作者的主要贡献所在）。

```
pybullet-rl-examples/
├── pyproject.toml                  # Poetry 项目配置（依赖、版本、作者）
├── README.md                       # 项目说明
├── CITATION.cff / LICENSE          # 引用与 MIT 许可证
│
└── gym_pybullet_drones/            # 主包
    ├── __init__.py
    │
    ├── assets/                     # 无人机 URDF 模型（cf2x/cf2p/racer）、贴图、GIF 动画
    │
    ├── utils/                      # 工具层
    │   ├── enums.py                # 枚举定义：DroneModel, Physics, ActionType, ObservationType
    │   ├── Logger.py               # 训练日志记录器
    │   └── utils.py                # 辅助函数（sync, str2bool 等）
    │
    ├── control/                    # 底层飞控算法
    │   ├── BaseControl.py          # 控制器基类
    │   ├── DSLPIDControl.py        # 级联 PID 控制器（位置→姿态→电机转速）
    │   ├── CTBRControl.py          # 计算力矩控制
    │   └── MRAC.py                 # 模型参考自适应控制
    │
    ├── envs/                       # Gymnasium 环境层
    │   ├── BaseAviary.py           # 环境基类（无人机群管理、PyBullet 步进）
    │   ├── BaseRLAviary.py         # RL 专用基类（观测/动作空间封装）
    │   ├── CtrlAviary.py           # 控制应用环境（本项目主要用于此）
    │   ├── HoverAviary.py          # 悬停任务环境（单机）
    │   ├── MultiHoverAviary.py     # 悬停任务环境（多机）
    │   ├── VelocityAviary.py       # 速度控制任务环境
    │   └── BetaAviary.py / CFAviary.py  # 其他变体
    │
    └── examples/                   # ★ 核心研发区（主要贡献）
        ├── marl_env.py             # 【核心】1v1 空战多智能体环境 (Drone1v1MARLEnv)
        ├── mappo_model.py          # 【核心】MAPPO 神经网络（Actor-Critic，支持权重迁移）
        ├── train_mappo.py          # 【核心】MAPPO 训练脚本（课程学习、分布式训练）
        ├── test_marl.py            # 模型测试脚本（压力测试 / 可视化测试）
        ├── tacview_logger.py       # Tacview ACMI 战术回放记录器
        ├── eval_tacview.py         # Tacview 评估辅助
        ├── eval_history_ghosts.py  # 历史轨迹幽灵回放
        ├── minimal_tac.py          # 最小化 Tacview 示例
        ├── mission_v1/v2/v3.py     # 已训练模型的演示脚本
        ├── basic/                  # 入门教程（learn.py / play.py / pid.py 等）
        ├── start/                  # 进阶教程（rl_frame.py / tactical_test_v1.0~v4.2）
        └── process/                # 历史实验记录（按日期编号的测试脚本）
```

### 核心文件详解

| 文件 | 功能说明 |
|------|----------|
| `examples/marl_env.py` | **1v1 空战多智能体环境**：完整的飞行动力学、战术几何计算、课程学习调度、奖励函数 |
| `examples/mappo_model.py` | **MAPPO 神经网络**：集中式 Critic + 分散式 Actor，支持动作空间扩展时的自动权重迁移 |
| `examples/train_mappo.py` | **MAPPO 训练主脚本**：Ray RLlib 分布式训练、3阶段课程学习、标准定标靶考核、TensorBoard 监控 |

---

## 三、运用的工具和主要方法

### 3.1 仿真与物理建模

- **PyBullet 物理引擎**：提供刚体动力学、碰撞检测、3D 可视化渲染。

- **自定义质点飞行动力学模型**：不依赖 PyBullet 原生的旋翼空气动力学，自建基于欧拉积分的 3-DOF 质点动力学：
  - 纵轴过载 $n_x$（加速/减速）
  - 法向过载 $n_n$（转弯/爬升/俯冲）
  - 滚转角 $\mu$（转弯方向控制）

- **一阶惯性延迟**（$\tau_g = 0.4\,\text{s}$）：模拟真实飞机过载建立过程，平滑控制量过渡。

- **V-n 包线限制**：升力随速度平方变化 $\propto (V/V_{\text{corner}})^2$，包含失速速度（60 m/s）与极速限制（400 m/s），确保飞行参数物理合理。

- **物理平滑过渡**：通过 P 控制器平滑逼近滚转角，滚转率上限 180°/s。

### 3.2 强化学习方法

#### MAPPO（Multi-Agent PPO）

基于 Ray RLlib 的集中式训练分散式执行（CTDE）架构：

| 组件 | 输入 | 网络结构 | 输出 |
|------|------|----------|------|
| **Actor**（策略网络） | 局部观测 (19维) | Linear(19→256) → Tanh → Linear(256→256) → Tanh → Linear(256→13) | 13个离散动作的 logits |
| **Critic**（价值网络） | 全局状态 (26维) | Linear(26→256) → Tanh → Linear(256→256) → Tanh → Linear(256→1) | 标量 Value |

- **局部观测 (19维)**：敌机相对位置(3) + 本机速度(3) + 本机姿态(3) + 本机角速度(3) + 本机高度(1) + 敌机速度(3) + 战术几何角 ATA/AA/HCA(3)
- **全局状态 (26维)**：双方各13维（位置3 + 四元数4 + 速度3 + 角速度3）

#### 自对弈（Self-Play）

攻击机和目标机双方策略同时训练（`policies_to_train=["policy_attacker", "policy_evader"]`），形成持续博弈演化。训练采用非对称学习率设计，攻击机略高以应对日益狡猾的目标。

#### 3阶段课程学习（Curriculum Learning）

| 阶段 | 初始距离 (m) | 高度范围 (m) | 目标机速度系数 | 目标机 G 系数 | 告警半径 (m) | 描述 |
|------|-------------|-------------|---------------|--------------|-------------|------|
| **Stage 1** | 600–1000 | 1500–2000 | 0.50 | 0.333 (≈3G) | 1500 | 新手村：固定靶/呆板靶 |
| **Stage 2** | 1000–1500 | 1800–2500 | 0.65 | 0.55 (≈5G) | 3000 | 中距拉锯：解禁机动能力 |
| **Stage 3** | 1500–2500 | 2200–3200 | 0.85 | 0.85 (≈7.7G) | 10000 | 长程高空对决：近乎全对称生死斗 |

#### 标准定标靶考核

阶段晋升时使用确定性动作靶，彻底切断目标机神经网络，保证考核的绝对客观性：
- **Stage 1 毕业标准**：对直飞靶（动作 0）实测击杀率 ≥ 70%
- **Stage 2 毕业标准**：对 2G 盘旋靶（动作 10）实测击杀率 ≥ 70%
- **Stage 3**：双方最强神经网络互搏，持续记录数据

每 10 次训练迭代进行 50 局的确定性压测，达标后自动晋级并保存里程碑模型。

### 3.3 战术空战建模（BFM）

#### 13个离散动作

基于基本战斗机机动（Basic Fighter Maneuvers）定义：

| 索引 | 动作 | 参数 $(n_x, n_n, \mu)$ | 战术含义 |
|------|------|------------------------|----------|
| 0 | 匀速直飞 | (0, 1G, 0°) | 巡航/待机 |
| 1 | 加速直飞 | (2, 1G, 0°) | 追击冲刺 |
| 2 | 减速直飞 | (-2, 1G, 0°) | 战术减速 |
| 3 | 暴力跃升 | (0, 8G, 0°) | 急跃升抢占高度 |
| 4 | 暴力俯冲 | (0, -8G, 0°) | 急俯冲逃逸 |
| 5 | 左转跃升 | (0, 8G, +60°) | 3D 左转爬升 |
| 6 | 右转俯冲 | (0, -8G, -60°) | 3D 右转俯冲 |
| 7 | 右转跃升 | (0, 8G, -60°) | 3D 右转爬升 |
| 8 | 左转俯冲 | (0, -8G, +60°) | 3D 左转俯冲 |
| 9 | 缓和右转 | (0, 2G, -60°) | 水平右盘旋 |
| 10 | 缓和左转 | (0, 2G, +60°) | 水平左盘旋 |
| 11 | 缓和跃升 | (0, 3G, 0°) | 末端俯仰微调 |
| 12 | 缓和俯冲 | (0, -3G, 0°) | 末端俯冲微调 |

#### 战术几何计算

- **ATA（天线偏角 / Antenna Train Angle）**：本机机头与目标视线的夹角 —— 衡量瞄准质量（1 = 完美瞄准）
- **AA（方位角 / Aspect Angle）**：敌机机头与视线的夹角 —— 衡量阵位优势（1 = 处于敌机正后方 6 点钟死角）
- **HCA（航向交叉角 / Heading Crossing Angle）**：双方机头夹角 —— 1 = 同向伴飞，-1 = 迎头对冲

#### 近炸引信（CPA）结算

- **动能撞击**：距离 < 50m → 击杀，固定 5000 分
- **CPA 近炸**：距离 ≤ 300m 且开始脱离 → 击杀，2000 基础分 + 按脱靶量线性分级（最高额外 3000 分）
- 设有暖机帧保护（`macro_step > 2`），防止开局奇异碰撞

#### GPWS 近地警告

- 攻击机：< 300m 且下坠速度 > 5 m/s → 强制给最大过载拉起、强制改平
- 目标机：< 800m 且下坠速度 > 5 m/s → 同上（阈值更高，留有更大安全余量）

### 3.4 奖励函数工程

#### 攻击机奖励（Attacker Reward）

共 8 个子项，构成密集的战术指引信号：

| 奖励项 | 设计意图 | 权重/计算 |
|--------|----------|-----------|
| **靠近奖励** | 距离缩近给分，增大惩罚 | `+0.1*Δdist`（靠近）/ `-0.03*Δdist`（远离）|
| **跟踪奖励** | 综合 ATA + AA + HCA + 碰撞角 + 侧滑惩罚 | ATA前半球 `4.0*cos`、锁定额外 `+2.0`、后半球 `-5.0*cos` |
| **时间惩罚** | 逼迫速战速决 | 从 `-0.5/s` 线性递增至 `-2.5/s` |
| **接近率奖励** | 速度矢量指向目标，引导前置拦截 | `0.08*closing_speed`，末端 400m 内提升至 `0.15*closing_speed` |
| **高度优势奖励** | 高于目标且机头对准时奖励；低于目标时惩罚 | 优势 `0.001*dz`，劣势可达 `-0.08*dz` |
| **软地板警告** | 防钻地 | 高度 < 200m 时指数级惩罚，叠加下坠速度惩罚 |
| **能量惩罚** | 防失速悬停 | 速度 < 150m/s 且非爬升时，`-0.5*(150-V)` |
| **终端击杀** | 动能撞击或 CPA 近炸 | 2000–5000 分 |

#### 目标机奖励（Evader Reward）

围绕"生存"展开：

| 奖励项 | 设计意图 |
|--------|----------|
| **存活底薪** | `+0.5/s`，鼓励尽量多活一秒 |
| **逃逸奖励** | 距攻击机 < 告警半径时，拉开距离奖励 `2.0*Δdist` |
| **角度破坏** | 被锁定（ATA > 0.5）时惩罚，`-(cos_ata)²*5.0` |
| **边界约束** | 低空 < 1000m 指数级惩罚 + 下坠速度惩罚；高空 > 3700m 软边界惩罚 |
| **超时成功** | 存活至 240s 回合结束，`+500` |

双方为**零和博弈**：击杀时攻击机 +5000，目标机 -5000。

### 3.5 辅助工具

| 工具 | 功能 |
|------|------|
| **TensorBoard** | 实时监控：双方奖励、胜率/坠毁率/越界率/超时率、策略熵、课程阶段、全局回合事件 |
| **Tacview ACMI** | 导出标准空战战术回放格式（坐标系从 PyBullet 对齐至 Tacview），可用 Tacview 软件进行专业 3D 事后分析 |
| **五模式运镜** | ① 经典尾随 ② 战术俯视地图 ③ 动态狗斗 ④ 目标锁定抵近 ⑤ 全局上帝视角 |
| **3D 可视化** | 三层高空参考网格（1500m/3000m/4500m）、半透明引信杀伤圈、巨型雷达标记球、红/黄 3D 轨迹尾迹、高度投影线 |
| **实时 HUD** | 飞机上方显示距离、空速、ATA、碰撞角偏差；目标机显示空速、滚转角 |

---

## 四、目前已做到的事项

| 类别 | 已完成事项 |
|------|-----------|
| 基础框架 | ✅ 在 gym-pybullet-drones 基础上搭建了完整的 RL 训练框架 |
| 多智能体环境 | ✅ 实现了 1v1 空战 MAPPO 环境，符合 PettingZoo / RLlib 多智能体接口规范 |
| MAPPO 神经网络 | ✅ 实现了集中式 Critic + 分散式 Actor 的 CTDE 架构 |
| 模型权重迁移 | ✅ 实现了动作空间扩展时的自动权重补齐（`load_state_dict` 外科手术式注入） |
| 自对弈训练 | ✅ 攻击机与目标机双方策略同时训练，形成持续博弈演化 |
| 3阶段课程学习 | ✅ 实现了从"固定靶" → "机动靶" → "全对称搏杀"的渐进训练体系 |
| 标准定标考核 | ✅ 阶段晋升使用确定性动作靶，确保考核客观公正 |
| 奖励函数精调 | ✅ 多轮迭代优化了包含追捕、能量、高度、防坠毁等多维度的密集奖励函数 |
| 飞行动力学 | ✅ 自建包含过载控制、一阶惯性延迟、V-n 包线、失速/极速限制的质点动力学模型 |
| 13动作 BFM | ✅ 定义了基于基本战斗机机动理论的 13 种离散战术飞行动作 |
| CPA 近炸结算 | ✅ 实现了基于最近接近距离（脱靶量）的分级击杀奖励机制 |
| Tacview 回放 | ✅ 支持导出 ACMI 格式供专业战术分析软件进行 3D 事后回放 |
| TensorBoard 监控 | ✅ 完整的训练指标可视化面板 |
| 多视角 3D 渲染 | ✅ 5 种相机模式 + 战术网格 + HUD 信息叠加 + 雷达标记 |
| 模型测试 | ✅ 支持 Headless 百场无渲染压力测试和带 GUI 单局可视化测试两种模式 |
| 入门/进阶教程 | ✅ `basic/` 和 `start/` 目录下有从零基础到深度定制的全套教学脚本 |
| 模型演示 | ✅ `mission_v1/v2/v3.py` 可加载已训练模型进行飞行演示 |

### 当前训练状态

> 正在 Stage 1–3 的课程学习框架下，从 `mappo_run_0610_1346` 检查点继续训练。
>
> **目标**：
> - 在每个阶段使攻击机对标准定标靶的实测击杀率达到 **70%** 以上
> - 逐级解禁目标机的速度、机动能力和战术视野
> - 最终实现 Stage 3 全对称条件下的高水平 1v1 空战博弈

---

> **致谢**：核心仿真环境基于 [learnsyslab/gym-pybullet-drones](https://github.com/learnsyslab/gym-pybullet-drones) 的优秀工作。作者的修改主要集中在 `examples/` 目录下的强化学习算法与目标追踪逻辑。
